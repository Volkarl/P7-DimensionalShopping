{
  "name": "DimentionalShoppingRestAPI",
  "version": "1.0.0",
  "description": "A rest API used for Dimentional Shopping",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "keywords": [
    "node",
    "restful",
    "api"
  ],
  "author": "sw706",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.2",
    // Web framework for nodejs, a layer that we use for HTTP requests 
    "morgan": "^1.9.1"
    // HTTP request logging for nodejs, useful for debugging 
  },
  "devDependencies": {
    "nodemon": "^1.18.7"
  }
}
